ODE traffic simulator

This simulator includes five traffic-flow simulations, corresponding to the models described in the project report. Simulation 3 is solved numerically using a numerical ODE method, while the remaining simulations are solved analytically.

The simulations are used to run each traffic model against real-world traffic data and compare the predicted results with the observed data."""